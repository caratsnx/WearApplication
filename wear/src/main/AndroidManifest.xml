<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.kushagra_standard.wearapplication">

    <uses-feature android:name="android.hardware.type.watch" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
>        <uses-library
            android:name="com.google.android.wearable"
            android:required="true" />
        <!--
               Set to true if your app is Standalone, that is, it does not require the handheld
               app to run.
        -->
        <meta-data
            android:name="com.google.android.wearable.standalone"
            android:value="true" />

        <activity
            android:name=".StartScreen"
            android:label="ABNAMRO Wear">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".show_list_function"
            android:label="@string/title_activity_show_list_function"></activity>
        <activity
            android:name=".balanceactivity"
            android:label="@string/title_activity_balanceactivity"></activity>
        <activity
            android:name=".actvity_pay"
            android:label="@string/title_activity_actvity_pay"></activity>
        <activity
            android:name=".creategoal"
            android:label="@string/title_activity_creategoal">

        </activity>
    </application>

</manifest>